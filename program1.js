/**
 * @param {string} s
 * @return {boolean}
 */
var isValid = function(s) {
    let counts = {}
    for(i=0; i<s.length; i++){
        switch(s[i]){
          case '(':
            counts[]
        }
    }

};

module.exports = { isValid };


